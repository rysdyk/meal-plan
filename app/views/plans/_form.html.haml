= simple_form_for @plan do |f|
  = f.input :name
  = f.input :notes
  = f.simple_fields_for :recipes do |r|
    = r.input :name
    = r.input :description
    = r.input :servings
    = r.input :calories
  = f.button :submit, class: 'button'
